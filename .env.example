# Fact Knowledge Layer - Environment Configuration
# Optional: Provide an API key if you wish to use cloud LLM-assisted extraction & reasoning.
# The system runs fully offline with deterministic semantic extraction if no API keys are provided.

# Google Gemini API Key (Optional)
GEMINI_API_KEY=

# OpenAI API Key (Optional)
OPENAI_API_KEY=

# Server Configuration
PORT=8000
HOST=0.0.0.0
DEBUG=True
